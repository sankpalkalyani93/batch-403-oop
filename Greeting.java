package com.example;

// compilable class => part of execution but will not execute on its own.
// this type of class will not have main method
public class Greeting {

	String message = "hello";
	
	public void displayMessage() {
		System.out.println(message);
	}
}
